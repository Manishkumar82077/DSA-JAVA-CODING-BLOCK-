import java.util.Arrays;

class insertion_sort_algo{
    public static void main(String[] args) {
        int[] arr = {23,76,85,445,34,64,56};
        System.out.println( (Arrays.toString(arr));
        insertion_sort(arr);
        System.out.println( (Arrays.toString(arr));
    }

    private static void insertion_sort(int[] arr) {
        
    }
}